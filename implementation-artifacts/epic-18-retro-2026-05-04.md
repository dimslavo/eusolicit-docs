# Epic 18 Retrospective — Trust Center & Compliance Posture

**Date:** 2026-05-04
**Facilitator:** Amelia (Developer)
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev), Deb (Project Lead)
**Epic Status at Retro:** `done` in sprint-status — 3/3 stories marked `done` in sprint-status; 1/3 story files say `Status: done` (18-2 ✅; 18-0 ❌ `Status: review`; 18-1 ❌ `Status: review`)

---

## Epic 18 Summary

| Metric | Value |
|--------|-------|
| Stories in sprint-status as `done` | 3/3 (18-0, 18-1, 18-2) |
| Story files with `Status: done` | **1/3** — only 18-2 reconciled; 18-0 and 18-1 still read `Status: review` |
| Story files with `REVIEW: Approve` confirmed | 18-0: ✅ (pass-2); 18-1: ✅ (pass-3); 18-2: ✅ (pass-2) |
| Total story points | 5 + 5 + 3 = **13** |
| Total review passes | 2 (18-0) + 3 (18-1) + 2 (18-2) = **7 passes** |
| Improvement vs E17 | 7 passes vs 18 in E17 — **61% reduction** |
| ATDD checklist in `test_artifacts/` | 18-0: ✅ `atdd-checklist-18-0-*.md` ALL GREEN; 18-1: inline §4.7 only; 18-2: inline §4.7 only |
| NFR assessment run | **NO** (stale E16 report — never updated for E17 or E18) |
| Traceability matrix regenerated | **NO** (stale E16 matrix, carried forward) |
| TEA review scores | **NOT RUN** (13th consecutive epic) |
| k6 performance baseline | **NOT RUN** (13th consecutive epic) |
| Dependabot / OWASP scan | **NOT CONFIGURED** (13th consecutive epic) |
| WeasyPrint native libs in test env | **NO** — Pango/Cairo absent, 12 tests in 18-1 marked `@pytest.mark.skip` (environmental) |
| E17 ATDD RED-phase un-skip (AP17-C2) | **PARTIAL** — 18-0 achieved full GREEN ATDD checklist; 18-1/18-2 used inline §4.7 only |
| Epic functional delivery | All 3 Trust Center pillars delivered: ✅ public route + MDX (18-0), ✅ PDF artefact pipeline (18-1), ✅ DPA notification flow (18-2) |

---

## Dialogue

Amelia (Developer): "Epic 18 is the Trust Center — the public compliance page that procurement committees need before they even talk to Sales. Three stories. Thirteen points. Seven review passes. That's less than half E17's pass count — a genuine improvement."

Charlie (Senior Dev): "Seven is still too many for thirteen points, but it's progress. E17 was a disaster at 18 passes. What drove the improvement?"

Amelia (Developer): "A few things. The stories were more tightly scoped. The anti-pattern fence was actively referenced — 42 rows by the end of 18-2. And 18-0 actually shipped a real ATDD checklist in `test_artifacts/`. First one since Epic 16."

Dana (QA Engineer): "That ATDD checklist for 18-0 was GREEN on every P0 test. That's what a proper story close looks like. But 18-1 and 18-2 still relied on inline §4.7 — which means I can't run the traceability matrix against them."

Alice (Product Owner): "From a product perspective — we hit the M2 deadline. The Trust Center is live, artefacts are downloadable, sub-processor change notifications are wired. The compliance chain from YAML change to DPA email works end to end."

Elena (Junior Dev): "The 18-0 → 18-1 → 18-2 chain was well-designed. Each story handed off exactly what the next story needed: `compute_diff()` and `yaml_content_hash()` from 18-0 were reused in 18-2 without reimplementation. That's the fence working as intended."

---

## Part 1: Epic 18 Review

### What Went Well

Amelia (Developer): "Let me pull the wins from the story records."

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: high`
**Trust Center end-to-end chain: 18-0 → 18-1 → 18-2 delivered without broken interface.** All three stories compose correctly: 18-0's `compute_diff()` + `yaml_content_hash()` → reused in 18-1 CI step + reused again in 18-2 publisher. 18-1's WeasyPrint renderer + signed-URL API → consumed by 18-2's DPA email body (`dpa_download_url` with graceful null-safe fallback per D-6). Zero rework at story boundaries. This is the correct multi-story dependency chain pattern. Reference: 18-0 AC-9 CR-6, 18-2 AC-1 §1, 18-2 AC-3 D-6.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: high`
**`yaml_content_hash()` as content-addressable idempotency key — stable across commit cycles.** Introduced in 18-0 CR-6 (replacing unstable git short-SHA). The 8-char `sha256(yaml_bytes)[:8]` hash is deterministic: run-locally → commit → push → CI all produce the same key. Downstream consumers (18-2 SETNX idempotency key `notification:dispatched:subprocessor:{changelog_sha}:{admin_user_id}`, changelog prepend guard, k6-day re-run protection) all depend on this stability. Regression tests: `test_yaml_content_hash_is_stable_for_identical_bytes` + `test_idempotency_survives_commit_cycle_simulation`. Reference: 18-0 CR-6, 18-2 AC-10.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: high`
**First GREEN ATDD checklist in `test_artifacts/` since Epic 16 (18-0).** Story 18-0 produced `test_artifacts/atdd-checklist-18-0-*.md` with all P0 tests GREEN at story close. This is the correct pattern: a standalone ATDD artifact in `test_artifacts/` that the traceability matrix and TEA gate can reference. The prior 4 epics (17, 16 partially) relied on inline §4.7 fill inside the story file, making cross-story coverage analysis impossible. Reference: ATDD checklist file, AP17-C2.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: high`
**ACK-after-fanout + SETNX idempotency in Redis Stream consumer — verified correct (18-2).** The notification consumer `subprocessor_consumer.py` follows the S16 M6 claim-on-success pattern: per-recipient `SETNX notification:dispatched:subprocessor:{changelog_sha}:{admin_user_id}` TTL 86400, ACK only after all recipient fan-out completes. Test `test_ack_called_only_after_fanout_complete` verified in code review pass 2. Anti-pattern fence #40 held. Reference: 18-2 AC-3, 18-2 pass-2 review.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Anti-pattern fence at 42 items — institutional memory compounds correctly.** Each Epic 18 story carried forward all prior fence rows and added net-new rows: 18-0 added #18–#24 (compile-time MDX, YAML path, no-DB-table-for-subprocessors, fail-fast changelog, no cross-route-group leakage, static vs. dynamic MDX boundary, frontend-side placeholder route). 18-1 added #25–#36 (WeasyPrint co-existence, S3 factory pattern, PDF idempotency). 18-2 added #37–#42 (no-reimplement-compute_diff, no-cross-import-_PAID_TIERS, PascalCase event_type literal, ACK-after-fanout, no-parallel-tracking-table, locale-default-bg). Each new story that reads the fence avoids previously discovered mistakes at zero cost. Reference: 18-0 §4.6, 18-1 §4.6, 18-2 fence table.

**[PATTERN]** `IMPACT: prompt_adjustment` `SEVERITY: medium`
**Mid-story dependency discovery resolved in-flight without stall (18-2 DEP-1).** Story 18-2 discovered during dev pass that `User.locale_preference` column was declared in the notification service's ORM mirror but never DDL'd in `client-api`. The dev agent added migration `062_add_locale_preference_to_users.py` immediately, without creating a separate story or stalling. This is the correct mitigation pattern when a missing prerequisite is discovered mid-story and the fix is small and scoped. Reference: 18-2 DEP-1, sprint-status line 317.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Review pass efficiency: 7 total passes vs. 18 in E17 — first-pass quality improving.** 18-0: 2 passes. 18-1: 3 passes (complex WeasyPrint + S3 + cross-tenant auth). 18-2: 2 passes. The improvement reflects better pre-review scope coverage: dev agents are completing more ACs before the first review pass. E17 averaged 4.5 passes/story; E18 averaged 2.3/story. Reference: sprint-status comments for all 3 stories.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**`trust_render_allow_company_admin_fallback` opt-in flag defaults False — production deny-by-default security posture.** When the cross-tenant authority model for PDF rendering was ambiguous (staff-admin allow-list vs. `is_company_admin`), the 18-1 reviewer correctly required the fallback to be opt-in, defaulting to deny. This is the correct posture for any privilege-adjacent feature: explicit opt-in, not opt-out. Reference: 18-1 B1 Round 3, `BaseServiceSettings.trust_render_allow_company_admin_fallback`.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Compile-time MDX for static surfaces; `next-mdx-remote/rsc` only for dynamic content — boundary maintained.** 18-0 correctly distinguishes: compliance posture, ISO roadmap = compile-time `@next/mdx` (auditability). Sub-processor changelog = `next-mdx-remote/rsc` (read at request time from a file that can change between deploys). Anti-pattern fence #23 locked this boundary. Source-inspection ATDD asserts the distinction. Reference: 18-0 AC-7, AC-11 §3, fence #23.

Charlie (Senior Dev): "I want to add one more: the 25-cell tier×status matrix in 18-2. That's the most comprehensive negative-test coverage we've done for a notification feature. Only `paid_tier × {active, trialing}` produces fan-out. Every other cell is asserted to produce zero notifications. That test is going to catch regressions."

Alice (Product Owner): "And locale-default-bg in the notification. Anti-pattern #42 — if we hadn't explicitly locked that, the platform would silently English-spam Bulgarian admins on every sub-processor change. That's an Art. 28 compliance detail that's easy to miss."

---

### Challenges and Anti-Patterns

Amelia (Developer): "Now the hard part. Let's be honest."

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: config_tuning` `SEVERITY: critical`
**Story files 18-0 and 18-1 still show `Status: review` — AP17-C5 (13th occurrence, AP18-01).** Despite the sprint-status correctly showing `done` for both stories, the story files themselves were not updated to `Status: done`. Only 18-2 has the correct `Status: done` in the story file header. This is the same administrative reconciliation gap that has appeared in every epic since E09. **Root cause:** the two-gate close (AP17-C1) correctly gates on `REVIEW: Approve` before sprint-status transition, but the _story file header_ update is not enforced as part of the same atomic operation. Fix: the `2b-dev-story-verify` phase must write `Status: done` to the story file simultaneously with the sprint-status transition. Reference: 18-0 story file line 3, 18-1 story file line 3.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: config_tuning` `SEVERITY: critical`
**k6 performance baseline absent — 13th consecutive epic (AP18-02).** The `/trust` route is public-facing and crawl-traffic exposed. The PDF artefact endpoint (`GET /api/v1/trust/artefacts/{slug}`) triggers a `head_object` S3 call + presigned URL generation on every request. Neither surface has been load-tested. `inj-02-k6-performance-baseline` has been `ready-for-dev` since E13 (12 consecutive carry-forwards, now 13). The E16 retro declared it a "hard gate for E17 kickoff" — that gate was ignored, and this retro is repeating the same finding. Fix: E19 kickoff MUST be gated on `load-test-results.md` containing non-dash values for at minimum: (1) `GET /en/trust` response time p95, (2) `GET /api/v1/trust/artefacts/{slug}` response time p95. Reference: E16/E17 retro same action, `inj-02` sprint-status.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: config_tuning` `SEVERITY: critical`
**TEA review gate absent — 13th consecutive epic (AP18-03).** Not a single TEA review was produced for any Epic 18 story. The `inj-03-tea-review-backlog` story has been `ready-for-dev` for 13 consecutive epics without execution. The E17 retro fix (embed TEA as `2b-dev-story-verify` gate) was not implemented. Fix: identical to AP17-C4 — TEA score ≥ 80/100 is a required pre-condition in `2b-dev-story-verify` before senior code review is triggered. Standalone `inj-03` should be removed from sprint-status; the gate must be mechanical. Reference: E17 AP17-C4, E14/E11 same action.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: high`
**NFR assessment not regenerated for E18 — stale E16 report is the only one in `test_artifacts/` (AP18-04).** Epic 18 introduces: a public-facing Next.js route (`/trust`), a WeasyPrint HTML→PDF renderer (`eusolicit_common.document_generation.weasyprint_renderer`), an S3 signed-URL issuance path in `client-api`, and a Redis Stream consumer in `notification`. None of these have been evaluated against PRD NFRs (p95 ≤ 200ms for REST, GDPR Art. 28 compliance, security posture of signed-URL TTL, WeasyPrint memory usage under concurrent renders). The E16 NFR report is 37 days stale. Fix: `bmad-testarch-nfr` must run for Epic 18 surfaces before Epic 19 kickoff. Reference: E17 AP17-C6, E16/E17 retro same action.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: high`
**ATDD checklist artifacts only created for 18-0 — 18-1 and 18-2 relied on inline §4.7 (AP18-05).** The improvement from E17 is real (18-0 has a proper `test_artifacts/` ATDD file, all GREEN) but incomplete. Stories 18-1 (complex backend: WeasyPrint + S3 + cross-tenant auth) and 18-2 (notification consumer + CI publisher + grants migration) both have their test design embedded in the story file's §4.7 section only. This means the TEA gate, traceability matrix, and NFR assessment cannot reference them. Fix: every story that modifies backend code must produce a standalone ATDD checklist in `test_artifacts/`. The inline §4.7 section should remain as a _draft_ — but it must be promoted to a standalone file before the story enters review. Reference: AP17-C2.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: high`
**`test-design-epic-18.md` never created — 6th consecutive epic with inline-fill workaround (AP18-06).** The inline §4.7 fill pattern has now been used in: S15-1, S16-0, 17-0, 17-1, 17-2, 17-3, 18-0, 18-1, 18-2. It is being treated as a permanent solution rather than a workaround. Without an epic-level test design, cross-story coverage analysis is impossible, the traceability matrix cannot be seeded with risk priorities, and TEA reviews lack a risk-taxonomy source. Fix: `test-design-epic-N.md` is a required deliverable in `test_artifacts/` as part of the first story's preparation phase. AP17-H4 re-fires. Reference: AP17-H4.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: medium`
**WeasyPrint native library dependencies (Pango/Cairo) absent from test environment — 12 environment-skipped tests in 18-1 (AP18-07).** Story 18-1 has 12 test cases skipped with `@pytest.mark.skip(reason='native WeasyPrint stack unavailable — install Pango/Cairo via apt-get')`. These test the core PDF rendering path (`render_html_to_pdf()` output = bytes starting with `b"%PDF"`, A4 stylesheet apply, DocumentRenderError on bad CSS). The production CI image presumably installs these dependencies, but the local development environment does not. Fix: `eusolicit-app/infra/docker-compose.test.yml` (or a Makefile target `make install-test-deps`) must install `libpango-1.0-0`, `libcairo2`, `libgdk-pixbuf2.0-0` for local test runs. Add a `make test-weasyprint` target that explicitly requires these deps, documented in `packages/eusolicit-common/README.md`. Reference: 18-1 test suite note.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: medium`
**18-1 Known Deviations D6–D11 deferred indefinitely — hardening sprint never materialises (AP18-08).** Seven deferred items from 18-1 Round 2: D6 (DoS surface on GET artefact endpoint), D7 (run_in_executor thread leak), D8 (head_object 403 silent re-PUT), D9 (validator ImportError silent bypass), D10 (SOURCE_DATE_EPOCH setdefault race), D11 (_safe_url_fetcher symlink/URL-decode), plus pre-existing D1–D5. These were deferred to "S18.02 hardening sprint" — but 18-2 was explicitly scoped to NOT absorb them (18-2 sprint-status note). The hardening sprint exists in documentation only; no story has been created for it. Fix: inject `18-3-trust-center-hardening` story with D6/D7/D8 as P0 acceptance criteria (DoS surface + thread leak are security/reliability concerns, not cosmetic). Reference: 18-1 Known Deviations section.

Elena (Junior Dev): "AP18-07 is the one that bit me during a local test run. I ran `make test` and saw 12 failures in 18-1. It took me 20 minutes to realize they were environmental, not code regressions."

Charlie (Senior Dev): "That's exactly the failure mode. Environmental skips that look like failures are worse than actual failures because they erode confidence in the test suite."

Amelia (Developer): "Good example of why the skip reason needs to be explicit. 18-1 did add the WeasyPrint stack probe so it skips cleanly — but the local install path is still undocumented."

---

## Part 2: E17 Action Item Follow-Through

| E17 Action | E18 Status |
|-----------|-----------|
| AP17-C1: Two-gate story-close verification | ✅ **Partially resolved** — `done` in sprint-status only after Approve; BUT 18-0/18-1 story files still `Status: review` |
| AP17-C2: Un-skip 257 ATDD RED-phase tests (17.1/17.2/17.3) | ⏳ **Partial** — 18-0 achieved full GREEN ATDD checklist; 18-1/18-2 inline §4.7 only; E17 RED tests themselves still not mentioned as resolved |
| AP17-C3: k6 baseline for integrations-api | ❌ **Not executed** (13th consecutive miss) |
| AP17-C4: TEA review as `2b-dev-story-verify` gate | ❌ **Not implemented** |
| AP17-C5: Fix 4 story file headers (Status: done) | ❌ **Carried into E18** — now 18-0/18-1 also need fixing |
| AP17-C6: Run `bmad-testarch-nfr` for Epic 17 | ❌ **Not run** — stale E16 report persists |
| AP17-H1: testcontainers Redis Lua fixture for integrations-api | ❌ **Not addressed** |
| AP17-H2: Orchestrator epic-kickoff gate enforcement | ❌ **Not implemented** |
| AP17-H3: Escalation after 2 consecutive BLOCKED verdicts | ❌ **Not implemented** |
| AP17-H4: Epic-level test design in `1-create-story` | ❌ **Not executed** (AP18-06 fires) |
| AP17-H5: ATDD un-skip count in completion checklist | ✅ **Partially applied** — 18-0 achieved this; 18-1/18-2 did not |

**Notable:** AP17-C2 (un-skip E17 ATDD tests) was the critical action that E18 kickoff depended on. E18 proceeded without verifying E17's 257 skipped tests — the same gate-bypass pattern documented in AP17-09 (E17 bypassed E16 gates). AP17-H2 is the systemic fix; it has not been implemented.

---

## Part 3: Next Epic Preview (Epic 19)

**Epic 19: Outcome Telemetry & Renewal Proof** — 3 stories, 21 points.

Amelia (Developer): "Epic 19 builds on two critical E18 deliverables: the `eusolicit_common.document_generation.weasyprint_renderer` module (19-2 uses WeasyPrint for Outcome Brief PDFs) and the notification consumer pattern from 18-2 (19-2 uses CSM stall alerts). These dependencies are real and must be stable."

Charlie (Senior Dev): "WeasyPrint is the concern. 18-1 has 7 deferred KDs including a thread leak (D7) and a DoS surface (D6). If we build the Outcome Brief PDF generator on top of `weasyprint_renderer` without fixing D7, we'll have two callers of a leaking thread pool."

Alice (Product Owner): "And 19 also depends on E12 (Analytics — materialized views) and E14 (workspace). Those are established but the ORM mirror pattern from 18-2 will be reused in 19-0 for cross-schema read access."

**Dependencies on E18 that must be stable:**
1. `eusolicit_common.document_generation.weasyprint_renderer.render_html_to_pdf()` — used directly in 19-2 S19.02. Known Deviations D7 (thread leak) is a pre-condition risk.
2. `eusolicit_common.document_generation.trust_s3.upload_trust_artefact()` — upload pattern potentially reused for Outcome Brief S3 storage in 19-2.
3. Notification consumer pattern (`subprocessor_consumer.py` → reuse for CSM stall alerts in 19-2).
4. `PAID_TIERS` mirror constants pattern (anti-pattern #38) — 19-2 CSM alerts may fan out to tenant_admin tier.

**Preparation needed before E19 kickoff:**
- CRITICAL: Inject `18-3-trust-center-hardening` story (D6 DoS + D7 thread leak as P0 ACs) before 19-2 consumes `weasyprint_renderer`.
- HIGH: Run `bmad-testarch-nfr` for E18 surfaces (the WeasyPrint renderer memory profile under concurrent renders is an E19 pre-condition for the Outcome Brief generator).
- HIGH: Fix 18-0 and 18-1 story file `Status:` headers (administrative).
- MEDIUM: Create `test-design-epic-19.md` as first-story preparation artifact.

---

## Action Items

### Critical — Must complete before E19 kickoff

**[ACTION]** `IMPACT: config_tuning` `SEVERITY: critical`
**AP18-C1: Inject `18-3-trust-center-hardening` story before E19-2 dispatch.** WeasyPrint D6 (DoS surface on GET artefact endpoint — no rate-limit, no request timeout) and D7 (run_in_executor thread leak — the POST handler's `asyncio.Lock + wait_for(300)` wrapper does not cancel the background thread on timeout) are P0 acceptance criteria for the hardening story. 19-2 depends on `weasyprint_renderer` — building on a leaking thread pool creates compounding tech debt. Story key: `18-3-trust-center-hardening`. Reference: AP18-08, 18-1 Known Deviations D6/D7.

**[ACTION]** `IMPACT: standards_update` `SEVERITY: critical`
**AP18-C2: Fix story file Status headers for 18-0 and 18-1.** Update `Status: review` → `Status: done` in both story files. Administrative fix; implementations are complete and reviewed. Owner: Orchestrator post-retro cleanup step. Reference: AP18-01, AP17-C5 (13th occurrence).

**[ACTION]** `IMPACT: config_tuning` `SEVERITY: critical`
**AP18-C3: k6 baseline for Trust Center endpoints — gate on E19 kickoff.** Endpoints: (1) `GET /en/trust` p95 response time (Next.js RSC with YAML + MDX server reads), (2) `GET /api/v1/trust/artefacts/{slug}` p95 response time (S3 head_object + presign), (3) `GET /bg/trust` i18n variant. Populate `load-test-results.md` with non-dash values before E19 first story enters `in-progress`. If `inj-02` is the vehicle, execute it. If not, create `18-4-trust-center-k6-baseline`. Reference: AP18-02, `inj-02-k6-performance-baseline` (ready-for-dev since E13).

**[ACTION]** `IMPACT: config_tuning` `SEVERITY: critical`
**AP18-C4: Run `bmad-testarch-nfr` for E18 surfaces.** Scope: WeasyPrint renderer memory profile under 3 concurrent renders, PDF byte-size limits for living artefacts, S3 presigned URL TTL enforcement (currently deferred KD in 18-1), notification consumer throughput under 200 concurrent admin recipients, GDPR Art. 28 30-day enforcement CI lint correctness. Output: `test_artifacts/nfr-report-epic-18.md`. Reference: AP18-04, AP17-C6 (unfulfilled).

### High — Complete within E19 sprint

**[ACTION]** `IMPACT: standards_update` `SEVERITY: high`
**AP18-H1: Create `test-design-epic-19.md` in `test_artifacts/` as part of first E19 story preparation.** Risk priorities for E19: R-019-1 (materialized view refresh under concurrent inserts — concurrency safety), R-019-2 (cross-workspace analytics bypass for `tenant_admin` — scope leakage), R-019-3 (WeasyPrint Outcome Brief PDF idempotency — SOURCE_DATE_EPOCH determinism), R-019-4 (CSM stall alert fan-out idempotency — SETNX same as 18-2 pattern). Cross-story integration touchpoints: 19-0 outcome-capture endpoint → 19-1 dashboard query keys must match. Reference: AP18-06, AP17-H4.

**[ACTION]** `IMPACT: standards_update` `SEVERITY: high`
**AP18-H2: Install WeasyPrint native dependencies in local test environment.** Add `libpango-1.0-0`, `libcairo2`, `libgdk-pixbuf2.0-0` installation to: (a) `eusolicit-app/Makefile` as `make install-native-test-deps` target, (b) `packages/eusolicit-common/README.md` "Local Development" section with `sudo apt-get install` instructions. The 12 skipped tests in 18-1 must un-skip locally. Reference: AP18-07.

**[ACTION]** `IMPACT: standards_update` `SEVERITY: high`
**AP18-H3: Produce standalone ATDD checklist files in `test_artifacts/` for 18-1 and 18-2.** Promote the inline §4.7 content from each story file to proper `test_artifacts/atdd-checklist-18-1-*.md` and `test_artifacts/atdd-checklist-18-2-*.md` files. Mark each test case as GREEN (implemented + passing) or RED (skipped/deferred). This unblocks the traceability matrix and TEA gate for E18. Reference: AP18-05.

**[ACTION]** `IMPACT: config_tuning` `SEVERITY: high`
**AP18-H4: TEA review gate — embed in `2b-dev-story-verify` before E19 story 0.** Same action as AP17-C4 (unfulfilled for 14th consecutive epic if not addressed). `bmad-tea` score ≥ 80/100 required before senior code review. Remove `inj-03-tea-review-backlog` standalone story from sprint-status if the gate is mechanically enforced. Reference: AP18-03, AP17-C4.

### Medium — E19 Backlog

**[ACTION]** `IMPACT: standards_update` `SEVERITY: medium`
**AP18-M1: Add testcontainers Redis Lua fixture to integrations-api conftest (AP17-H1 carry-forward).** The 3 Lua atomicity test files in integrations-api (`test_hubspot_rate_limit.py`, `test_pipedrive_rate_limit.py`, `test_salesforce_quota.py`) remain skipped with `@pytest.mark.skip(reason='testcontainers infra deferred')`. E15 pattern applies. Reference: AP17-H1.

**[ACTION]** `IMPACT: standards_update` `SEVERITY: medium`
**AP18-M2: Orchestrator epic-kickoff gate enforcement parses previous retro Critical section (AP17-H2 carry-forward).** Before the first story of E19 enters `in-progress`, the orchestrator must verify the "Critical — Must complete before E19 kickoff" items above. If any are unmet, Telegram alert is raised. Reference: AP17-H2.

---

## Readiness Assessment for Epic 19

| Dimension | Status |
|-----------|--------|
| Trust Center functional chain (18-0→18-1→18-2) | ✅ Stable — all 3 pillars delivered |
| Story file integrity | ❌ 18-0/18-1 at `Status: review`; need AP18-C2 fix |
| `weasyprint_renderer` stability | ⚠️ **7 KDs including D6 (DoS) + D7 (thread leak)** — must address before E19-2 uses it |
| ATDD coverage in `test_artifacts/` | ⚠️ 1/3 stories have standalone file (18-0 only); 18-1/18-2 inline only |
| k6 baseline | ❌ Not run (AP18-C3) |
| NFR assessment | ❌ Not run for E18 (AP18-C4) |
| TEA gate | ❌ Not enforced (AP18-H4) |
| WeasyPrint native libs local | ❌ Not installed (AP18-H2) |
| Traceability matrix | ❌ Stale E16 matrix; E17/E18 not covered |

**Conclusion:** E19 kickoff MUST be gated on AP18-C1 through AP18-C4. The Trust Center is functionally delivered and the GDPR Art. 28 compliance chain works. The cleanup is a mix of administrative (story headers), coverage (ATDD files, NFR), and reliability (WeasyPrint KDs) work. However, E19-2 directly depends on `weasyprint_renderer` — starting E19 before fixing D6/D7 means building the Outcome Brief PDF generator on a known-leaking foundation.

---

## Key Insights

1. **Review pass efficiency is improving, but ATDD coverage discipline is not yet consistent.** E18 averaged 2.3 review passes/story vs. E17's 4.5. This is real progress. But only 1/3 stories produced a formal ATDD checklist artifact — the other two relied on inline §4.7 fill. The improvement must extend to ATDD file creation, not just first-pass review quality.

2. **The anti-pattern fence is working.** 42 rows of fenced anti-patterns, actively referenced across all three E18 stories. Zero fence violations detected in code review. Specific wins: PascalCase `SubprocessorChanged` (fence #39), ACK-after-fanout (fence #40), locale-default-bg (fence #42), compute_diff reuse (fence #37). This is institutional memory operating correctly.

3. **Content-addressable idempotency keys are more robust than event-based keys.** The `yaml_content_hash()` pattern — introduced in 18-0 as a CR-6 fix and propagated through 18-1 and 18-2 — is stable across commit cycles, CI re-runs, and whitespace edits. Any future idempotency-keyed operation should use content hashing rather than event timestamps or git SHAs.

4. **Multi-story epic chain design works when interface contracts are explicit.** 18-0 → 18-1 → 18-2 had zero rework at boundaries because: (a) 18-0 exposed `compute_diff()` and `yaml_content_hash()` as importable functions; (b) 18-1 exposed `dpa_download_url` with a documented null-safe fallback; (c) 18-2 consumed both without reimplementation. The fence pattern (#37, #38) enforced no-reimplementation. This is the reference design for multi-story epic chains.

5. **The k6/TEA/NFR triple-gate miss has become structural, not incidental.** 13 consecutive epics without k6. 13 without TEA. 2 without NFR. These are not items that keep getting "forgotten" — they are not mechanically enforced. Until the `2b-dev-story-verify` phase includes them as blocking gates, they will continue to be skipped. The fix is structural (orchestrator config), not behavioral.

6. **Story file `Status:` header is the simplest possible administrative step and it keeps failing.** The two-gate close (AP17-C1) correctly gates sprint-status transitions on `REVIEW: Approve`. But writing `Status: done` to the story file header requires a separate operation that is consistently missed for 2/3 stories. The fix is one line of orchestrator logic: on sprint-status transition to `done`, patch the `Status:` header in the story file atomically.

---

## Commitments

| Commitment | Owner | When |
|------------|-------|------|
| Inject `18-3-trust-center-hardening` (D6+D7) | Dev agent | Before E19-2 backlog |
| Fix 18-0/18-1 `Status: review` → `Status: done` | Orchestrator post-retro | Immediate |
| Execute k6 baseline for Trust Center endpoints | inj-02 or 18-4 | Before E19 kickoff |
| Run `bmad-testarch-nfr` for E18 | NFR agent | Before E19 kickoff |
| Create `test_artifacts/atdd-checklist-18-1-*.md` and `atdd-checklist-18-2-*.md` | Dev agent | Before E19 kickoff |
| Embed TEA gate in `2b-dev-story-verify` | Orchestrator config | Before E19 first story |
| Create `test-design-epic-19.md` | ATDD agent | As part of E19-0 preparation |
| Install WeasyPrint native deps in Makefile + README | Dev agent | Before E19-2 dev pass |

---

*Generated by bmad-retrospective on 2026-05-04*
*All [ACTION], [PATTERN], [ANTI-PATTERN] markers are formatted for ChangeEvaluator auto-processing.*
