# Epic 15 Retrospective — Per-Bid Pricing SKU & Pro+ Tier
**Date:** 2026-04-27
**Facilitator:** Amelia (Developer)
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev), Deb (Project Lead)
**Epic Status at Retro:** Sprint-status shows `done` — see CRITICAL INTEGRITY ISSUE below

---

## Epic 15 Summary

| Metric | Value |
|--------|-------|
| Stories in sprint-status as `done` | 3 / 3 |
| Stories with confirmed code review Approve | 2 / 3 |
| Stories with story file `Status: done` | 1 / 3 |
| Story 15-0 (Pro+ Tier Definition) | ✅ Confirmed done |
| Story 15-1 (Per-Bid Pricing Tiers) | ❌ CRITICAL — "Changes Requested" unresolved; 8 BLOCKING findings |
| Story 15-2 (Usage Lua Metering Bypass) | ⚠️ Code review Approved, story file not updated to done |
| NFR assessment | **FAIL — 3 HALT conditions** |
| Traceability coverage (P0) | 96.9% |
| Traceability coverage (total) | 92% |
| ATDD checklists created | 2 (S15.0, S15.2) — S15.1 none |
| TEA review scores | **NOT RUN (11th consecutive epic)** |
| k6 performance baseline | **NOT RUN (9th consecutive epic)** |
| Stripe circuit-breaker | **NOT IMPLEMENTED (9th consecutive epic)** |

---

## ⚠️ CRITICAL INTEGRITY ISSUE: Epic 15 Is Not Actually Done

**Story 15-1 (`15-1-per-bid-pricing-tiers-stripe-checkout-integration-admin-config`) is marked `done` in `sprint-status.yaml` but:**

1. The story file carries `Status: review` (not `done`).
2. The Senior Developer Review verdict is **"REVIEW: Changes Requested"** — never resolved, never re-reviewed, never approved.
3. The review identified **8 BLOCKING findings** that break the per-bid purchase flow at runtime:
   - B1: `PerBidCheckoutRequest.tier_id` vs AC/frontend `pricing_tier_id` — every Purchase button returns 422
   - B2: Backend returns `{"tiers": [...]}` wrapper but frontend expects bare array — picker throws
   - B3: Migration 050 seed rows have wrong BG labels, empty `feature_stack`, non-null fake `stripe_price_id`
   - B4: CHECK constraints (`name IN (...)`, `price_eur_cents > 0`) missing from migration
   - B5: AC 2 indexes `ix_add_on_purchases_pricing_tier_id` and composite workspace+opportunity index missing
   - B6: Cross-tenant test uses raw SQL inserts instead of canonical fixtures — same anti-pattern as S15.0 B2
   - B7: Frontend calls `GET /api/v1/billing/per-bid/status` which does not exist — Per-Bid SKU badge never renders
   - B8: Stripe `success_url` hardcodes `/bg/workspace/default/` — non-BG users and non-default workspaces land on 404

4. The NFR assessment confirmed: *"Story 15.1 is not demonstrably operational."*

**This is the worst instance of the story-status-drift anti-pattern in project history.** Previous occurrences (E12, E13, E14) involved stories that had been code-review Approved but the story file not updated. Story 15-1 was never Approved. The sprint-status reflects a false completion signal.

**BEFORE EPIC 16 STARTS:** Story 15-1 must complete a full review-fix pass and receive code review Approve. Epic 16 (`Integrations API Service Bootstrap`) builds on the payment infrastructure that 15-1 is supposed to establish. Proceeding without fixing 15-1 embeds known runtime-breaking bugs into the production path.

---

## Part 1: Epic 15 Review

### What Went Well

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Story 15-0 clean execution with strong carry-forward codification** — S15.0 went through exactly one review round, resolved 3 blocking + 4 medium findings systematically, and delivered 49 tests all green. The Dev Notes section §"Previous Story Learnings" explicitly named the E14 anti-patterns by story number and AC number. This is the correct pattern: prior retro findings embedded in the story Dev Notes rather than relying on the dev agent to consult project-context.md independently.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: high`
**Lua atomicity proof via concurrent test (`asyncio.gather(10_000)`)** — S15.2's `test_concurrent_check_and_increment_enterprise` drives 10,000 concurrent `check_and_increment` calls and asserts `len(set(results)) == 10_000`. This is a sharper assertion than `max == 10_000` alone — it proves every counter value from 1 to 10,000 was assigned exactly once. This pattern is now the reference implementation for all Redis counter atomicity proofs. Reference: `test_usage_metering_bypass.py`.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Workspace-scoped bypass key dual-lookup** — S15.2 correctly resolved the workspace-scoped / company-scoped ambiguity in the usage gate bypass by: (a) extracting `workspace_id` from the already-verified JWT in `get_usage_gate`, (b) checking `add_on_active:{workspace_id}:{opportunity_id}` first, falling back to `add_on_active:{company_id}:{opportunity_id}`. This matches the `scoped_id = workspace_id_str or company_id_str` pattern established by S15.1's webhook handler (`webhook_service.py:699-700`). The pattern is now consistent across all three sides: webhook write, bypass check, status read.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**Explicit tier branch over implicit fall-through** — S15.0 review-fix M4 added an explicit `if self.user_tier == "pro_plus": return True` branch in `opportunity_tier_gate.is_in_scope` with an explanatory comment. The principle: whenever a new tier is inserted between existing tiers, every `if/elif` chain or fall-through default that implicitly assigns behaviour must get an explicit branch. Silent fall-through is a latent bug when any future story tightens the default.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**`_PRO_PLUS_TIERS_LIST` as explicit literal, not `list(frozenset)`** — S15.0's Debug Log #2 caught that `list(PRO_PLUS_TIERS)` from a frozenset produces non-deterministic order across Python versions and runs. Any list derived from a set for use in a WHERE clause, ordering, or display MUST be an explicit literal `["pro_plus", "enterprise"]`. This pattern applies to all `_*_TIERS_LIST` constants in `tier_gate.py`.

**[PATTERN]** `IMPACT: standards_update` `SEVERITY: medium`
**ATDD traceability at 96.9% P0 coverage on completed stories** — For the stories that had ATDD checklists (S15.0, S15.2), the traceability matrix showed 96.9% P0 FULL coverage. This is the highest P0 coverage seen in any epic. When ATDD checklists are written and activated, the test coverage follows.

---

### Challenges and Anti-Patterns

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: config_tuning` `SEVERITY: critical`
**Story marked `done` in sprint-status without code review Approve — worst instance to date** — S15.1 is `done` in sprint-status with a pending "Changes Requested" verdict and 8 unresolved BLOCKING findings. Prior occurrences (E12, E13, E14) involved approved-but-file-not-updated drift. This is the first time a story with an explicit rejected review verdict was closed. The orchestrator story-close workflow **must** verify: (1) story file `Status: done`, AND (2) Senior Developer Review section contains `REVIEW: Approve` — not `REVIEW: Changes Requested`. Both checks must pass before sprint-status is updated.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: standards_update` `SEVERITY: critical`
**Bespoke test fixtures in S15.1 B6 — 4th consecutive epic, 5th occurrence this epic** — S15.1's `test_per_bid_checkout_cross_tenant.py` rolled a `_seed_company_with_bid_manager()` helper using `text("INSERT INTO client.client_workspaces ...")`, `text("INSERT INTO pipeline.opportunities ...")`, etc. The story's own Dev Notes §"Previous Story Learnings" item 1 read: *"DO NOT rebuild ... Story 15.0 had to refactor 4 test files for this — DO NOT repeat."* The instruction was in the story file and was not followed. Root cause: dev agents write the most locally-convenient fixture rather than searching for the canonical helper. The canonical fixture import block must be pre-populated in the test file template so the agent cannot write the first line without seeing it.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: critical`
**FE↔BE request/response contract untested at integration level** — S15.1's `PerBidCheckoutRequest.tier_id` vs the correct `pricing_tier_id` was only caught by a human code reviewer reading two files in parallel. The backend integration tests (`test_per_bid_checkout_happy_path.py`) locked in the wrong field name because they were written against the backend implementation rather than the AC spec. Rule: any story that introduces a new POST body or response schema shared between FE and BE MUST include a contract test that (a) posts the request exactly as the FE client would, and (b) asserts the response exactly as the FE client parses it. The AC field names are the contract, not the implementation.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: critical`
**Migration seed rows deviating from AC spec (4th consecutive epic)** — S15.1 migration 050 seeded BG labels `'Стандартен'/'Грант модул'/'Ентърпрайз'` instead of the AC-specified `'Стандарт'/'С грантов модул'/'Корпоративен пакет'`; seeded empty `feature_stack = '{}'` instead of the per-tier JSON; seeded fake non-null `stripe_price_id` strings instead of `NULL`. This matters because (a) the BG locale UX is driven by the seeded labels, (b) S15.2's metering bypass derives feature gating from `feature_stack`, (c) the non-null `stripe_price_id` defeats the `tier_not_configured` 422 branch. Pattern fix: the migration idempotency test (AC 15) must assert each seeded column value against the AC spec table — not just row count.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: high`
**Locale and workspace_id hardcoded in Stripe success/cancel URLs** — `billing_service.create_per_bid_checkout_session()` used `f"{settings.frontend_url}/bg/workspace/default/..."`. The project uses next-intl with `[locale]` routing and workspace-scoped URLs introduced in Epic 14. Any service function that builds a frontend URL MUST accept `locale: str` and `workspace_id: UUID | str` as parameters and construct the URL dynamically. Template: `f"{settings.frontend_url}/{locale}/workspace/{workspace_id}/..."`. This must become a standard note in the billing service template.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: high`
**Non-existent endpoint wired into frontend success flow** — AC 10's "Unlocked: Per-Bid SKU active" badge called `GET /api/v1/billing/per-bid/status?opportunity_id={id}`, which has no backend implementation. Frontend stories must explicitly list each API endpoint they call, with a corresponding backend story or AC that creates it. If the endpoint is out of scope for the current story, the frontend must use a local cache signal (e.g., check the Redis active key result from the checkout success query param) rather than a non-existent polling endpoint.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: high`
**Admin-api cache invalidation using a new `aioredis.from_url()` connection instead of `get_redis_client` dep** — `admin_api/api/v1/pricing_tiers.py::_invalidate_pricing_tier_cache` opened its own Redis connection, bypassing the existing `get_redis_client` dependency. Per the Architecture §8: all Redis access in FastAPI handlers MUST go through the registered `get_redis_client` dependency so it can be overridden in tests and shares the connection pool. Direct connection creation in route handlers is a BLOCKING code review finding.

**[ANTI-PATTERN]** `[ACTION]` `IMPACT: prompt_adjustment` `SEVERITY: medium`
**Two sources of truth for ai_summary Professional tier limit (M5 deferred from S15.0)** — `usage_gate.FEATURE_TIER_LIMITS["ai_summary"]["professional"] = 50` but `tier_access_policies.ai_summaries_limit = 100` for Professional. The metering gate and the DB-persisted policy row disagree on the limit. This pre-existed Story 15.0 but was explicitly deferred to this retrospective. Resolution: `usage_gate.FEATURE_TIER_LIMITS` should be eliminated and all limit values read from `tier_access_policies` at startup (or cached). If that's out of scope, a CI test must assert `usage_gate.FEATURE_TIER_LIMITS["ai_summary"][tier]` == `tier_access_policies.ai_summaries_limit` for every tier row.

---

### Previous Epic Retro Action Item Follow-Through (Epic 14)

| E14 Action Item | Status | Impact if Not Done |
|-----------------|--------|--------------------|
| Bespoke fixtures: canonical import block in test template | ❌ NOT DONE | S15.1 B6 — same anti-pattern repeated |
| Story-status drift: orchestrator gate on story file Status | ❌ NOT DONE | S15.1 marked done without approval |
| TEA review embedded as story AC (not separate story) | ❌ NOT DONE | 11th consecutive epic with zero TEA scores |
| k6 baseline for E15 endpoints | ❌ NOT DONE | NFR HALT condition #3 |
| Stripe circuit-breaker on billing service | ❌ NOT DONE | NFR HALT condition #2 |
| RBAC matrix restore (14.2 carry-forward) | ❌ NOT DONE | workspace RBAC under-tested going into E16 |
| NFR assessment run for E15 | ✅ DONE | Revealed 3 HALT conditions |
| Documentation-vs-code: grep File List in review | ⚠️ PARTIAL | S15.0 improved; S15.1 M3 skipped schema file per spec |

**6 out of 8 E14 action items carried forward with zero progress.**

---

## Part 2: Next Epic Preparation (Epic 16)

### Epic 16 Overview
**Title:** Integrations API Service Bootstrap (Slack/Teams webhook configuration + alert routing)
**First story:** `16-0-integrations-api-service-bootstrap-slack-teams-webhook-configuration-alert-routing`
**Status:** backlog

### Hard Prerequisites Before Epic 16 Kickoff

These are not recommendations — Epic 16 CANNOT start until they are resolved:

1. **[BLOCKER] Complete Story 15-1 review-fix pass**
   - Owner: Dev agent
   - Fix: B1–B8 from senior dev review; full re-run; get Approve verdict
   - Update story file to `Status: done`; update sprint-status to `done`
   - Epic 16's alert routing builds on the per-bid purchase event contracts established in S15.1

2. **[BLOCKER] Fix Story 15-2 status drift**
   - Story file says `Status: review` but code review is Approved
   - Update story file to `Status: done` (trivial — no code change needed)
   - Owner: Orchestrator / operator

3. **[BLOCKER] Execute k6 baseline for E15 payment endpoints**
   - `POST /api/v1/opportunities/{id}/per-bid/checkout` (Stripe call, latency unvalidated)
   - `GET /api/v1/billing/per-bid/pricing-tiers` (Redis cache, first-request DB query unvalidated)
   - PRD NFR-PF-1: p95 ≤ 200 ms. Payment endpoint likely does NOT meet this (Stripe API round-trip > 200 ms typical) — must be documented with an approved deviation or the timeout budget restructured.
   - Owner: INJ-02 `inj-02-k6-performance-baseline` story (E13 carry-forward, now 9 epics overdue)

4. **[BLOCKER] Implement Stripe circuit-breaker on `billing_service.py`**
   - Apply `circuit_breaker(retry(http_factory))` pattern to all `asyncio.to_thread(stripe.*)` call sites (at minimum: `create_checkout_session`, `create_per_bid_checkout_session`, `create_customer_portal_session`, `modify_subscription_item`)
   - Reference: E04 two-layer resilience pattern; E08 retro action item (7th consecutive deferral)
   - Owner: INJ-01 or a new hardening story injection

5. **[RECOMMENDED] Resolve M5 ai_summary limit mismatch**
   - Reconcile `usage_gate.FEATURE_TIER_LIMITS["ai_summary"]["professional"] = 50` with `tier_access_policies.ai_summaries_limit = 100`
   - Can be addressed within first E16 story or as a surgical patch
   - Owner: Dev agent as prep task

### Preparation for Epic 16 Execution

1. **Dev-story template update (for E16):**
   - Add canonical fixture import block (pre-populated) to Testing Requirements
   - Add gate: "If story introduces FE↔BE API contract, include a contract test asserting AC field names"
   - Add gate: "If story adds Stripe SDK calls, apply circuit_breaker(retry()) — non-negotiable"
   - Add gate: "If story builds Stripe checkout URLs, locale + workspace_id must be parameters"

2. **Migration seed test template:**
   - Add migration-level test template that asserts each seeded column value against the story's AC spec table

3. **TEA review must run before code review:**
   - TEA review (`bmad-tea`) must be triggered by the dev agent before requesting senior code review
   - Score ≥ 80/100 required; embed as explicit task in dev-story prompt

---

## Action Items

### P0 — Must complete before Epic 16 kickoff

| # | Action | Owner | Success Criteria |
|---|--------|-------|------------------|
| A1 | Complete Story 15-1 review-fix (B1–B8) | Dev agent | Code review returns "Approve"; full `make test-service SVC=client-api` + `admin-api` quoted |
| A2 | Update Story 15-2 file status to `done` | Orchestrator | `grep "^Status: done" 15-2-*.md` returns true |
| A3 | Execute k6 baseline for per-bid checkout and pricing-tier endpoints | INJ-02 | `load-test-results.md` populated with actual p50/p95/p99 values |
| A4 | Apply Stripe circuit-breaker to `billing_service.py` checkout call sites | Dev agent / injection story | `circuit_breaker(retry())` wraps all `asyncio.to_thread(stripe.*)` in billing path |
| A5 | Update orchestrator story-close workflow: require `Status: done` in file AND `REVIEW: Approve` in story | Orchestrator config | `epic-15-retrospective: done` in sprint-status; E16 stories cannot be closed without both gates |

### P1 — High priority for early Epic 16 stories

| # | Action | Owner | Success Criteria |
|---|--------|-------|------------------|
| A6 | Resolve ai_summary limit mismatch (M5) | Dev agent | `usage_gate.FEATURE_TIER_LIMITS["ai_summary"]["professional"]` removed or equals 100 to match `tier_access_policies` |
| A7 | Restore workspace RBAC matrix (14.2 carry-forward, traceability CONCERNS) | Dev agent | `test_workspace_rbac.py` contains 60-case parametrised matrix; traceability scan passes |
| A8 | Add canonical fixture import block to dev-story template | Config / standards update | S16 first story uses `create_company_pair` / `register_and_verify_with_role` without a BLOCKING finding |
| A9 | Add FE↔BE contract test requirement to fullstack story template | Standards update | Any story spanning FE+BE includes at least one test that sends the AC-specified request field names |
| A10 | Add TEA review as explicit dev-agent task in dev-story prompt | Prompt update | S16-0 includes `bmad-tea` invocation before requesting code review |

### Agreements

- **Epic 16 kickoff is gated on A1–A5 all complete.** The orchestrator MUST enforce this gate.
- **Story-close workflow will be updated to verify both story file status AND review verdict before writing `done` to sprint-status.**
- **`inj-03-tea-review-backlog` is retired.** TEA review is embedded as a mandatory dev-agent task step, not a separate story that never executes.
- **"M5 deferred to retrospective" is no longer an acceptable deferral path.** If a reviewer identifies a pre-existing inconsistency between sources of truth, it is injected as a P1 carry-forward story immediately, not deferred to retrospective.

---

## Epic 15 Readiness Assessment

| Dimension | Status | Notes |
|-----------|--------|-------|
| Story completion (code) | ⚠️ 2/3 confirmed | S15.1 "Changes Requested" unresolved |
| Testing quality | ⚠️ | S15.0/S15.2 strong; S15.1 has critical test gaps |
| Traceability (P0) | ✅ 96.9% | Best result to date |
| NFR gate | ❌ FAIL | 3 HALT conditions |
| Production readiness | ❌ NOT READY | S15.1 broken at runtime; Stripe CB absent; k6 absent |
| Story file / sprint-status alignment | ❌ 1/3 aligned | 15-1 and 15-2 file ≠ sprint-status |

---

## Key Takeaways

1. **The per-bid SKU feature (Epic 15's headline delivery) is not operational.** Story 15-1 must be completed before Epic 15's business value is realized.

2. **Bespoke fixture anti-pattern at 4th consecutive epic — structural fix required.** The instruction "do not rebuild fixtures" alone is insufficient; the canonical import block must be physically present in the test file at the moment of first write.

3. **When a story spans FE and BE, the AC field names are the contract.** Writing backend tests against the implementation rather than the AC locks in bugs invisibly.

4. **Migration seed rows must be validated against the AC spec table.** Row count is insufficient; column values must match.

5. **Story 15-0's execution quality is the model.** 1 review round, explicit anti-pattern codification in Dev Notes, 49 tests, strong ATDD coverage — this is what clean delivery looks like.

---

## Team Performance Summary

Epic 15 delivered the Pro+ tier infrastructure (S15.0) cleanly and closed the Lua atomicity gap (S15.2) with a strong concurrency proof. The per-bid SKU front-to-back feature (S15.1) was not completed. The recurring carry-forward items (k6, Stripe CB, TEA review, bespoke fixtures) remain unresolved after 9+ epics. Epic 16 cannot be productive until the S15.1 runtime bugs are fixed. The team pattern is clear: infrastructure stories (single service, well-scoped) execute well; fullstack stories with FE+BE+admin+migration+webhook+tests across multiple services require additional safeguards.

---

*Retrospective document generated by bmad-retrospective skill, 2026-04-27.*
